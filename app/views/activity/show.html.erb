
    <div id="wrapper">
      <nav class="navbar navbar-default navbar-fixed-top">
        <div class="navcontainer">
          <button type="button" href="#menu-toggle" id="menu-toggle" class="navbutton btn btn-lg navbar-right navbar-btn"><i class="fa fa-bars" aria-hidden="true"></i>
          </button>
        </div>

      </nav>

        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <ul class="sidebar-nav">
              <li class="sidebar-brand">
                  <a href="#">
                      Welcome <%= current_user.name %>
                  </a>
              </li>

                <li class="sidebar-brand">
                    <a href="#" id="#copylink" class="btn btn-primary copylink">
                        Invite your friends!
                    </a>
                </li>
                <li>
                    <a href="/profile">Back to Profile</a>
                </li>
                <li>
                  <a id="buttonloc" data-toggle="collapse" href="#updatepassword" aria-expanded="false" aria-controls="updatepassword">
                    Set new access key </a>
                </li>
                <div class="collapse" id="updatepassword">
                <li>
                  <%= form_for @activity, url: "/activity/#{@activity.id}/password", html: {class: "", role: "form", method: :patch} do |f| %>
                    <div class="updatekey">
                    <%= f.password_field :password, placeholder: "Access key" %>
                    </div>
                    <div class="updatekey">
                    <%= f.password_field :password_confirmation, placeholder: "Confirm access key" %>
                    </div>
                    <%= f.submit "Confirm", class: "btn btn-default updatekeybtn" %>

                  <% end %>
                </li>
                </div>
                <li>
                    <a href="#">User Overview:</a>
                </li>
                <%@activity.users.each do |user| %>
                <%= image_tag user.normalimage, class: (user == current_user ? 'avatar mypicture' : 'avatar') %>
                <li><%=user.username%></li>
                <% if @activity.hosts.find_by(userid: current_user.id) && @activity.hosts.find_by(userid: user.id) == nil %>
                <form id="makehost", data-remote = "true" >
                  <%= hidden_field_tag :userid, user.id %>
                  <%= hidden_field_tag :actid, @activity.id %>
                <%= submit_tag "Make Host", class: "btn btn-primary" %>
                </form>
                <% end %>
                <% end %>
                <li>
                    <a href="#">Logout</a>
                </li>

            </ul>
        </div>
        <!-- /#sidebar-wrapper -->

        <!-- Page Content -->
        <div class="">

        <div class="row-centered">
        <%= render "activitycard" %>
        <!-- <div class="piecharts">
        <div class="progress-pie-chart datepie" data-percent="0">
          <div class="ppc-progress datepie">
            <div class="ppc-progress-fill datepie"></div>
          </div>
          <div class="ppc-percents datepie">
            <div class="pcc-percents-wrapper datepie">
              <span>%</span>
            </div>
          </div>
        </div>

        <div class="progress-pie-chart timepie" data-percent="0">
          <div class="ppc-progress timepie">
            <div class="ppc-progress-fill timepie"></div>
          </div>
          <div class="ppc-percents timepie">
            <div class="pcc-percents-wrapper timepie">
              <span>%</span>
            </div>
          </div>
        </div>

        <div class="progress-pie-chart locpie" data-percent="0">
          <div class="ppc-progress locpie">
            <div class="ppc-progress-fill locpie"></div>
          </div>
          <div class="ppc-percents locpie">
            <div class="pcc-percents-wrapper locpie">
              <span>%</span>
            </div>
          </div>
        </div>
      </div> -->
        </div>

        <div id="page-content-wrapper">
            <div class="container-fluid">
              <div class="row row-centered">

                  <!-- <div class="activity">
                  <%= image_tag @activity.image.url(:medium) %>
                  <ul>
                    <li>Name: <%= @activity.name%></li>
                    <li>Description: <%= @activity.description%></li>
                    <li>Location: <%= @activity.location%></li>


                    <li>Date: <%= @activity.starts_at_date%></li>

                    <li>Time: <%= @activity.starts_at_time%>

                    </li>
                  </ul>
                  <% if @activity.hosts.find_by(userid: current_user.id) %>
                  <a href="#" class="btn btn-default" data-toggle="modal" data-target="#myModal">Change Details</a>
                  <% end %>


                </div> -->

              </div>



              <div class="row row-centered">
                <a href="#" class="btn btn-default" data-toggle="modal" data-target="#myModal">Change Details</a>
                <% if @activity.location_entries.where(votes: @activity.location_entries.maximum(:votes)).count > 1 %>

                <div class="votingresults">
                  <% @activity.location_entries.each do |d| %>
                  <% sorted = @activity.location_entries.sort_by &:votes %>
                  <% if d.votes == sorted.last.votes %>
                  <a class="btn btn-default"><%=d.location%></a>
                    <% end %>
                  <% end %>
                </div>
                <% end %>

                <% if @activity.date_entries.where(votes: @activity.date_entries.maximum(:votes)).count > 1 %>
                <div class="votingresults">
                  <% @activity.date_entries.each do |d| %>
                  <% sorted = @activity.date_entries.sort_by &:votes %>
                  <% if d.votes == sorted.last.votes %>
                  <a class="btn btn-default"><%=d.date.strftime("%a %d.%m.")%></a>
                    <% end %>
                  <% end %>
                </div>
                <% end %>

                <% if @activity.time_entries.where(votes: @activity.time_entries.maximum(:votes)).count > 1 %>
                <div class="votingresults">
                  <% @activity.time_entries.each do |d| %>
                  <% sorted = @activity.time_entries.sort_by &:votes %>
                  <% if d.votes == sorted.last.votes %>
                  <a class="btn btn-default"><%=d.time.strftime("%I:%M%p")%></a>
                    <% end %>
                  <% end %>
                </div>
                <% end %>

                <div class="buttonrow">
                <% if @activity.locationvoting == true %>
                <div class="votebutton">
                <a class="btn btn-lg btn-primary" role="button" id="buttonloc" data-toggle="collapse" href="#locationvoting" aria-expanded="false" aria-controls="locationvoting">
                  VOTE Location!
                </a>
                </div>
                <% end %>
                <% if @activity.datevoting == true %>
                <div class="votebutton">
                <a class="btn btn-lg btn-primary" role="button" data-toggle="collapse" href="#datevoting" aria-expanded="false" aria-controls="datevoting">
                  VOTE Date!
                </a>
                </div>
                <% end %>
                <% if @activity.timevoting == true %>
                <div class="votebutton">
                <a class="btn btn-lg btn-primary" role="button" data-toggle="collapse" href="#timevoting" aria-expanded="false" aria-controls="timevoting">Vote Time!</a>
                </div>

                <% end %>
              </div>
            </div>



              <div class="collapse " id="locationvoting">
                <div class="well locationcontent">
                  <%= render "locationvotes" %>
                </div>
              </div>

              <div class="collapse " id="datevoting">
                <div class="well datecontent">
                  <%= render "datevotes" %>
                </div>
              </div>

              <div class="collapse " id="timevoting">
                <div class="well timecontent">
                  <%= render "timevotes" %>
                </div>
              </div>

              <div class="container">
              <div class="row commentcontainer">
                <%= render "comments" %>
              </div>
                </div>


            </div>
        </div>
        <!-- /#page-content-wrapper -->

    </div>
    <!-- /#wrapper -->


  </div>














<div class="modal fade" tabindex="-1" role="dialog" id="myModal">
  <div class="modal-dialog ">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">Edit Activity</h4>
      </div>
      <div class="Intro"> </div>
      <div class="modal-body">
        <div class="container">
        <%= form_for @activity, url: "/activity/#{@activity.id}", html: {class: "form", role: "form", multipart: true, method: :patch} do |f| %>
          <div class="form-group">
          <%= f.label :name, "Name:" %>
          <%= f.text_field :name %>
          </div>
          <div class="form-group">
          <%= f.label :description, "Description:" %>
          <%= f.text_area :description %>
          </div>
          <div class="form-group">
          <%= f.label :location, "Location:" %>
          <%= f.text_field :location %>
          </div>
          <div class="form-group">
          <%= f.label :starts_at, "Date:" %>
          <%= f.date_select :starts_at %>
          <%= f.label :starts_at, "Time:" %>
          <%= f.time_select :starts_at %>
          </div>
          <div class="form-group">
          <%= f.label :image, "Image:" %>
          <%= f.file_field :image %>
          </div>
          <div class="">
            Allow Voting:
          </div>
          <div class="form-group">
          <%= f.label :locationvoting, "Location:" %>
          <%= f.check_box :locationvoting, {:class => "allowvoting", :style => "form"}, "true", "false"%>
          <%= f.label :datevoting, "Date:" %>
          <%= f.check_box :datevoting, {:class => "allowvoting", :style => "form"}, "true", "false" %>
          <%= f.label :timevoting, "Time:" %>
          <%= f.check_box :timevoting, {:class => "allowvoting", :style => "form"}, "true", "false" %>
          </div>
          <%= f.submit "Submit Changes", class: "btn btn-primary" %>

        <% end %>

        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
      </div>
    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->


<div class="modal fade" tabindex="-1" role="dialog" id="myModal2">
  <div class="modal-dialog modal-lg ">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">Vote</h4>
      </div>
      <div class="Intro"> </div>
      <div class="modal-body ">

      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
      </div>
    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->

<script>
$("#menu-toggle").click(function(e) {
    e.preventDefault();
    $("#wrapper").toggleClass("toggled");
});
</script>
